# =============================================================================
# Docker Environment Variables
# =============================================================================
# Copy this file to .env and update values for your deployment
#
# Usage:
#   cp .env.docker .env
#   # Edit .env with your production values
#   docker-compose up -d --build
# =============================================================================

# Backend API URL - IMPORTANT: Update this to your backend server address
# Examples:
#   - Local: http://localhost:4000
#   - Docker network: http://backend:4000
#   - Production: https://api.yourdomain.com
VITE_API_BASE_URL=http://localhost:4000

# Google OAuth Client ID (optional)
VITE_GOOGLE_CLIENT_ID=574265213050-gk0taidhlkjm15gughommnchceafaekc.apps.googleusercontent.com

# Port to expose the frontend on the host machine
FRONTEND_PORT=5173
